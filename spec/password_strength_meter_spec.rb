require 'spec_helper'

describe PasswordStrengthMeter do
  it 'has a version number' do
    expect(PasswordStrengthMeter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
